export interface ResourceArray {
  id: number;
  title: string;
  image: string;
  subtitle: string;
  description: string;
}

export interface Show {
  cssHelpers: boolean;
  icons: boolean;
  illustrations: boolean;
  animations: boolean;
  charts: boolean;
  images: boolean;
}
